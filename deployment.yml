apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-multitool-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ng-multi
  template:
    metadata:
      labels:
        app: ng-multi
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
      - name: multitool
        image: wbitt/network-multitool
        env:
        - name: HTTP_PORT
          value: "1180"
        - name: HTTPS_PORT
          value: "11443"
        ports:
        - containerPort: 1180
          name: http-port
        - containerPort: 11443
          name: https-port
